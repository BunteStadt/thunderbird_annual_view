# name: Deploy GitHub Pages
#
# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:

# permissions:
#   contents: read
#   pages: write
#   id-token: write

# concurrency:
#   group: pages
#   cancel-in-progress: true

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}

#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v4

#       - name: Assemble site
#         run: |
#           npm ci
#           npm run build:web
#           mkdir -p _site/src
#           cp index.html _site/
#           cp assets/feiertage_nrw.ics assets/ferien_nrw.ics _site/
#           cp -R icons _site/
#           cp -R dist/web _site/web

#       - name: Setup Pages
#         uses: actions/configure-pages@v5

#       - name: Upload site artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: _site

#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4
