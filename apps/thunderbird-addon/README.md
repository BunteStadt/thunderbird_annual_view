# Thunderbird Add-on

This folder contains the Thunderbird WebExtension add-on runtime sources:

- `src/` add-on UI and background scripts
- `experiments/` Thunderbird experimental API bindings
- `icons/` add-on icons

The release manifest is at repository root (`/manifest.json`) and points to files in this folder.
