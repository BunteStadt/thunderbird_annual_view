services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    stdin_open: true
    tty: true
    environment:
      DISPLAY: :99
      HOME: /root
    volumes:
      - .:/workspace
    command: ["bash"]
